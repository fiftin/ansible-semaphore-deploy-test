- name: get source code
  pause:
    seconds: 1

- name: PWD
  debug:
    msg: "{{ lookup('env', 'PWD') }}"

- name: build version {{ semaphore_task_version }}
  pause:
    seconds: 1

- name: copy artifacts to storage
  pause:
    seconds: 1
