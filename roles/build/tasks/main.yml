- name: get source code
  pause:
    seconds: 1

- name: print user_var
  debug:
    msg: "{{ user_var }}"

- name: print semaphore_vars
  debug:
    msg: "{{ semaphore_vars }}"

- name: build version {{ semaphore_vars.task_details.target_version }} of app
  pause:
    seconds: 1

- name: copy app to storage
  pause:
    seconds: 1
